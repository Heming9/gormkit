// Package gormkit provides small, composable helpers for GORM v2.
//
// The package includes database lifecycle management, context-aware
// transactions, a generic repository, reusable scopes, tenant isolation, and
// an optional Unix timestamp plugin. Applications can use an explicit
// Database instance or install one as the process default for legacy-style
// package functions.
package gormkit
